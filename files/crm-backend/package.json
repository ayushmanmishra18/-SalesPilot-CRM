{
  "name": "crm-backend",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api":    "npm run build --workspace=packages/shared && npm run dev --workspace=apps/api",
    "dev:worker": "npm run build --workspace=packages/shared && npm run dev --workspace=apps/worker",
    "build":      "npm run build --workspace=packages/shared && npm run build --workspace=apps/api && npm run build --workspace=apps/worker",
    "lint":       "npm run lint --workspaces --if-present"
  }
}
